# Python dependencies for training, dataset tools, and benchmark reporting.
#
# Install PyTorch separately so that the wheel matches the server's CUDA driver:
# https://pytorch.org/get-started/locally/
# The development environment uses torch 2.4.1 with CUDA 12.1.

numpy>=1.22,<2
opencv-python-headless>=4.5
tensorboard>=2.8
configargparse>=1.5
PyYAML>=5.3
pandas>=1.3
matplotlib>=3.3
uniplot>=0.10
